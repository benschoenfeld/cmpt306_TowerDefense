class_name ToolEnums

extends Node
## A node that allows access for multiple scenes to use the same tool enums.

## An [enum] that are the current tools the player has.
enum Tool {SHOVEL, WATERCAN, HOE, TARGET}
